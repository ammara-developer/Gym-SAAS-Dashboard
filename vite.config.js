/**
 * vite.config.js — Vite Build Configuration
 * Standard React + Vite setup. No extra changes needed.
 */

import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
});
